node_modules/
out/
dist/
release/
grok-build/
docs/spike-raw/
package-lock.json

# Swift build output. `npm run lint` runs `prettier --check .`, and Prettier
# reads only the root ignore file — so a bare `swift build` in `native/` breaks
# lint for the whole repository. `build.sh` and `test.sh` redirect their scratch
# path to keep this out of the tree, but a hand-run `swift build` still lands
# here (docs/phase-2-report.md §5.2, docs/phase-3-report.md §5.5).
native/.build/
native/build/

CHANGELOG.md
LICENSE
CODE_OF_CONDUCT.md
.github/**

# Long-form session reports. Prettier wraps tables and shell blocks into
# diffs that bury the content; they are not product source.
docs/handoff-insertion-2026-09-06.md
docs/report-insertion-2026-09-06.md
